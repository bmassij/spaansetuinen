Legacy HTML extern gearchiveerd

De legacy HTML-site (map `website/` en alle .html-bestanden) is extern gearchiveerd en is bewust verwijderd uit deze Git-repository. De content-engine gebruikt vanaf nu uitsluitend JSON-bestanden in de content-map (bijv. `spaansetuinen-next/content/`).

Reden: voorkomen van duplicatie, voorkomen dat Roo of andere tooling toegang heeft tot legacy HTML binnen deze repo.

Datum: 2026-01-18
